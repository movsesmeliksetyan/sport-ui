version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_ACE_STREAM_URL=http://192.168.0.99:6878/ace/manifest.m3u8
    volumes:
      - .:/app
    command: npm run start
